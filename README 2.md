# Bertie’s Books
A simple Node.js + Express web application used to display a bookshop catalogue and demonstrate server-side rendering using EJS templates.

### Features
- Displays a list of books stored in a database
- Renders dynamic HTML pages using EJS
- Styled using a simple custom CSS file (main.css)
- Includes routes for:
- Listing all books
- Searching for books (depending on your code)

### Technologies Used
- Node.js
- Express.js
- EJS 
- CSS for styling
- MySQL 

### Running the APP
npm install
node index.js
